{
  "name": "my-bedrock-addon",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:all",
    "build:hookshot": "npm --prefix hookshot_addon run build",
    "build:utility": "npm --prefix mining_utility_addon run build",
    "build:all": "npm run build:hookshot && npm run build:utility",
    "pack:hookshot": "npm --prefix hookshot_addon run pack",
    "pack:utility": "npm --prefix mining_utility_addon run pack",
    "pack:all": "npm run pack:hookshot && npm run pack:utility",
    "pack": "npm run pack:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@minecraft/math": "^2.4.0",
    "@minecraft/server": "^2.9.0",
    "@minecraft/server-ui": "^2.1.0"
  },
  "devDependencies": {
    "archiver": "^8.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
